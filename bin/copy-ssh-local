#!/bin/sh

# shellcheck source=./common.sh
source "common.sh"

identity="$(email-to-hash $ID_EMAIL)"
file="$HOME/.ssh/$identity.pub"

# pbcopy < "${HOME}/.ssh/${identity}.pub"
# echo "${HOME}/.ssh/${identity}.pub"

#if [ ! -f "$file" ]; then
#  log_error "File '$identity' does not exists!"
#fi

cat "$file"

log_success 'Export ssh key and copy the clipboard.'
