#!/bin/bash

# shellcheck disable=SC1091
# shellcheck source=../shell-common
source "${SHELL_HOME}/shell-common"

ip=$(curl --silent -disable https://icanhazip.com)
echo "$ip" | tr -d '\n ' | pbcopy
output "$ip" "info"
